/** Default post-login / app-open landing — Syllabus Tracker. */
export const APP_HOME_PATH = "/syllabus";

/** Former /home daily hub (feature grid, execution signals). */
export const APP_DASHBOARD_PATH = "/dashboard";

/** B2B institute portal overview (org admins / faculty). */
export const B2B_INSTITUTE_PATH = "/institute";
