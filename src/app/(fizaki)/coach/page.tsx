import { SurfacePlaceholder } from "@/components/fizaki/SurfacePlaceholder";

export default function FizakiCoachPage() {
  return (
    <SurfacePlaceholder
      title="FIZAKI Coach"
      subtitle="Grounded in your playbook progress and your live pipeline."
      points={[
        "Connects skill gaps to stuck deals",
        "Tells you what to drill now — with a script for tomorrow",
        "Scoped to your data only, never another brand's",
      ]}
    />
  );
}
